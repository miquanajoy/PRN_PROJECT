@page
@model PRN_Project.Pages.Admin.Categories.IndexModel


<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Category List</h2>
        </div>

        <div class="col-6">
            <a asp-page="upsert" class="btn btn-primary">
                <i class="bi bi-plus-circle-fill"></i>&nbsp; Create new category</a>
        </div>
    </div>

    <br />
    <br />

    <table id="DT_load" class="table table-bordered table-striped table-hover" style="width:100%">
        <thead>
            <tr>
                <th>
                    Category Name
                </th>
                <th>
                    Display Order
                </th>
                <th>
                    Edit/Delete
                </th>
            </tr>
        </thead>

        @*<tbody>
            @foreach(var obj in Model.categories)
            {
                <tr>
                    <td width="50%">@obj.Name</td>
                    <td width="30%">@obj.DisplayOrder</td>
                    <td width="15%">
                        <div class="w-75 btn-btn-group" role="group">
                        <!--Edit button-->
                        <a asp-page="Edit" asp-route-id="@obj.Id" class="btn btn-warning mx-2">
                            <i class="bi bi-pencil-square"></i>
                        </a>
                        <!--Delete button-->
                        <a asp-page="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2">
                            <i class="bi bi-trash3-fill"></i>
                        </a>
                        </div>
                    </td>         
                </tr>
            }
        </tbody>*@
    </table>
</div>

@section Scripts {
    <script src="~/js/category.js"></script>
}
